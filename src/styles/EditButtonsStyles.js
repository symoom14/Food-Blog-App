import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  saveButton: {
    paddingVertical: 14,
    borderRadius: 50,
    alignItems: "center",
    marginTop: 10,
  },
  saveButtonText: {
    fontSize: 16,
    fontWeight: "600",
  },
  deleteButton: {
    paddingVertical: 14,
    borderRadius: 50,
    alignItems: "center",
    marginTop: 10,
  },
  deleteButtonText: {
    fontSize: 16,
    fontWeight: "600",
  },
});

export default styles;
